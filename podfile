# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FTPWelch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FTPWelch

  target 'FTPWelchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FTPWelchUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'FilesProvider'
end
